
set FLASK_APP=run.py
set FLASK_ENV=development

flask db init
flask db migrate -m "Initial migration"
flask db upgrade

python run.py

http://localhost:5000

http://127.0.0.1:5000/login

Login using:

Username: admin

Password: admin123


from app import db
from app.models import School, User

school = School(name="Maahad Istiqama")
db.session.add(school)
db.session.commit()

admin = User(username="admin", email="admin@example.com", school_id=school.id)
admin.set_password("admin123")
db.session.add(admin)
db.session.commit()

 Grant all privileges on the database to school_user
GRANT ALL PRIVILEGES ON school_timetable_db.* TO 'school_user'@'localhost';
GRANT ALL PRIVILEGES ON school_timetabl_db.* TO 'school_user'@'localhost';
